package gauss

const (
	ErrorSquareMatrix    = "system matrix must be square"
	ErrorVectorDimension = "incorrect dimension of the vector of free terms"
	ErrorTriangleMatrix  = "for back substitution matrix must be triangular"
)
